import { Meta, Story } from '@storybook/addon-docs/blocks'
import { StyledPreview as Preview } from '../../../.storybook/decorator-components'
import LoginForm from './'
import Layout from '../layout'

<Meta title="Login Form" component={LoginForm} />

## Login Form

Login Form component. Uses [`Formik`](https://jaredpalmer.com/formik). This means it can be passed validation schemas.

<Preview>
  <Story name="Default" decorators={[story => <Layout>{story()}</Layout>]}>
    <LoginForm />
  </Story>
</Preview>

You can pass a different `header` message to the `<LoginForm>`. This will be displayed above the inputs.

<Preview>
  <Story
    name="Header message"
    decorators={[story => <Layout>{story()}</Layout>]}
  >
    <LoginForm header="Powered by Titus" />
  </Story>
</Preview>
