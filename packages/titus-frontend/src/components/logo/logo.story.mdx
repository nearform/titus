import { Meta, Story } from '@storybook/addon-docs/blocks'
import { StyledPreview as Preview } from '../../../.storybook/decorator-components'
import Logo from './'
import Layout from '../layout'

<Meta title="Logo" component={Logo} />

## Logo

Logo component for project.

<Preview>
  <Story name="Default" decorators={[story => <Layout>{story()}</Layout>]}>
    <Logo />
  </Story>
</Preview>
