---
name: Autocomplete
menu: titus-components
route: /titus-components/autocomplete
---

import { Playground, PropsTable } from 'docz'
import Autocomplete from './autocomplete'
import { countries } from './stories/countries'

# Autocomplete

The Autocomplete component provides typical autocompletion features using [Material UI] and [downshift].

It supports synchronous and asynchronous loading of list items.

## Properties

<PropsTable of={Autocomplete} />

## Typings Definitions

```ts
type DataItem = {
  value: string
  key: string
}

export interface AutocompleteProps {
  data?: Array<DataItem>
  inputValue?: string
  placeholder?: string
  id?: string
  filterType?: string
  maxResults?: number
  loading?: any
  items?: Array<DataItem>
  onInputChange?: (e: any) => void
  onChange?: (item: DataItem) => void
}
```

## Usage

This section shows how to use the Autocomplete component.

<Playground>
  <Autocomplete
    placeholder="I am a placeholder"
    data={countries}
    onChange={console.log}
  />
</Playground>
