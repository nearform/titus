{
  "name": "titus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap && lerna exec npm install",
    "create:env": "lerna run --parallel create:env",
    "lint:all": "lerna run --parallel lint",
    "test:all": "lerna run --parallel test",
    "start:all": "lerna run --stream start",
    "stop:all": "lerna run --stream stop",
    "clean:all": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
