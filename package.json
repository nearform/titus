{
  "name": "titus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build:components": "lerna exec --scope titus-components npm run build",
    "start:kitchen-sink": "lerna exec --scope titus-kitchen-sink npm start"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
