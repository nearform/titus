{
    "name": "titus-lib",
    "scripts": {
        "echo": "echo 'lib'",
        "test": "jest --config .jest.json --coverage"
    },
    "devDependencies": {
        "chalk": "^4.1.2",
        "jest": "27.3.1"
    }
}