{
    "name": "titus-lib",
    "scripts": {
        "clean": "rimraf node_modules/",
        "test": "jest --config .jest.json --coverage"
    },
    "devDependencies": {
        "chalk": "^4.1.2",
        "jest": "27.3.1"
    }
}