steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - "--tag=gcr.io/$PROJECT_ID/titus-storybook:$COMMIT_SHA"
      - "--file=.storybook/docker/Dockerfile"
      - .
images:
  - "gcr.io/$PROJECT_ID/titus-storybook:$COMMIT_SHA"
